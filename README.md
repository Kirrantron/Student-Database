# Student-Database
It is student database project developed using data structures in C, this project uses linked list to store the data which are created using dynamic memory allocation.
The code has a menu-driven function system which can be used based on the inputs given by the user during runtime. 
The file can be saved or loaded from memory anytime based on the request of the user.
